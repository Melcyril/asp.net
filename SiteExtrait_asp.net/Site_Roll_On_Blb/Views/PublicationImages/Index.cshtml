@model IEnumerable<Site_Roll_On_Blb.Models.PublicationImage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.urlPublicationImage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Publication.titrePublication)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.urlPublicationImage)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Publication.titrePublication)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idPublicationImage }) |
            @Html.ActionLink("Details", "Details", new { id=item.idPublicationImage }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idPublicationImage })
        </td>
    </tr>
}

</table>
